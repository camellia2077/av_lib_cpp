## [v0.4.6] - 2026-04-06

### 技术改进/重构 (Changed/Refactor)
* 重构 C++ 目录结构：将表现层从 `src/presentation/` 迁移到根目录 `apps/`，并拆分为 `apps/cli/` 与 `apps/gui/`。
* 迁移应用入口文件：将 `src/cmd_main.cpp` 调整为 `apps/cli/cmd_main.cpp`，将 `src/main.cpp` 调整为 `apps/gui/main.cpp`。
* 重构本地头文件引用路径：将 `#include "presentation/..."` 统一调整为 `#include "apps/..."`，并保持 `src/core/` 作为核心业务目录。
* 重构构建源清单：将 `cmake/sources.cmake` 中 CLI/GUI 源文件路径从 `src/presentation/...` 更新为 `apps/...`。

### 修复 (Fixed)
* 修复目录迁移后的编译可见性问题：在 `CMakeLists.txt` 中为 GUI/CLI 目标补充根目录头文件搜索路径，确保 `apps/...` 头文件可被正确解析。

