## 2026-04-06

### 技术改进/重构 (Changed/Refactor)
* 重构脚本入口为单一 Python 命令：将 `tools/script/build.py` 重命名为 `tools/script/run.py`，统一通过子命令分发构建与测试流程。
* 重构 `clang-tidy` 调用链：将 `cmake/clang_tools.cmake` 从 `--run-clang-tidy` 模式切换为 `python tools/script/run.py clang-tidy --build-dir ...`。
* 重构构建目录规范：将分散的 `build`、`build_fast`、`build_tidy` 口径统一为 `out/build/<mode>`，并统一产物目录为 `out/bin` 与 `out/lib`。
* 迁移第三方库路径：将 `lib/imgui` 调整为 `third_party/imgui`，并同步更新 `cmake/third_party.cmake` 引用路径。

### 新增功能 (Added)
* 新增 `run.py` 子命令 `test-core`，用于编排 C++ 核心测试的配置、构建与 `ctest` 执行。
* 新增 `run.py` 子命令 `smoke-cli`，用于编排 CLI 端到端冒烟流程。

### 弃用/删除 (Deprecated/Removed)
* 删除旧脚本入口与壳脚本：`tools/script/build.py`、`tools/script/build_fast.sh`、`tools/script/build_tidy.sh`、`tools/script/format.sh`、`tools/script/run.sh`、`tools/script/test_cli_smoke.ps1`。
